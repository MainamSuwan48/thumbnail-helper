{
  "name": "thumbnail-helper",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "concurrently \"bun run --filter server dev\" \"bun run --filter client dev\"",
    "build": "bun run --filter server build && bun run --filter client build",
    "start": "bun run --filter server start"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "typescript": "^5.7.0"
  },
  "trustedDependencies": ["esbuild", "sharp"]
}
